// components/Product.ts
export interface ProductData {
  id: number;
  title: string;
  description: string;
  price: number; // Adjust the type based on your actual data type for price
  product_image: string; // Assuming product_image contains the image URL
  // Add other properties as needed
}
